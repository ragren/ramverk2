build:
    tests:
        override:
            -
                command: 'npm run scrutinizer'
                coverage:
                    file: 'coverage/clover.xml'
                    format: 'clover'
